---
issue: 52
type: added
---
Add the opt-in `graphify-mcp` module (installs the verified `graphifyy[mcp]` local stdio MCP server extra) and `omes graphify mcp health` (a read-only status check with a `not_applicable` default and a documented CLI fallback) that never touches hermes-gateway.
