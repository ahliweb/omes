---
issue: 50
type: added
---
Add the `graphify` module (per-user `uv tool`/pipx install of the `graphifyy` CLI, PEP 668-safe, with version pinning and rollback) and `omes graphify update`/`omes graphify uninstall` extension commands.
