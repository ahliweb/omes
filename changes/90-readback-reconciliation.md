## Control jobs

- Require exact equality between backend `desired` and `observed` state when a read-back supplies both fields, rejecting incomplete or partial reconciliation instead of reporting mutation success.
