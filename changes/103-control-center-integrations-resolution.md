## Control Center and integrations resolution

- Synchronize delivery sequence tables in `docs/control-center-and-integrations.md` and `docs/coolify-adapter.md` with active OMES implementations for issues #90 through #97.
- Isolate test runner stdin in `tests/py/agent/test_cli.py` and `tests/py/content/test_cli_worker_flow.py` to prevent interactive terminal hangs.
