---
issue: 55
type: security
---
Add docs/graphify-privacy.md (local vs. provider-backed extraction, credential handling, provenance, deletion), `omes graphify init-ignore` (safe `.graphifyignore`/`.gitignore` defaults, idempotent) and `omes graphify purge` (removes only marker/manifest-owned OMES artifacts, never user-authored content), plus one row each in docs/security.md and docs/threat-model.md.
