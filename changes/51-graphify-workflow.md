---
issue: 51
type: added
---
Add `omes graphify run` (path-validated, mode-gated code/semantic extraction with a provenance sidecar) and `omes graphify skill install`/`uninstall` (a Hermes `/graphify` skill wrapper).
