---
issue: 81
type: added
---
Add opt-in `conservative`/`strict` systemd hardening profiles (`OMES_HERMES_HARDENING`) for the Hermes gateway, with predictive `module_check` warnings, bounded-timeout auto-rollback on a failed restart, and `omes doctor` reporting.
