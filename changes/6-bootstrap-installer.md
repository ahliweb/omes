---
issue: 6
type: added
---
Add the OMES bootstrap installer: `bin/omes` CLI (`check`/`install`/`status`/`modules`/`version`/`help`, stubs for `doctor`/`backup`/`restore`/`update`/`uninstall`), the `lib/omes/*` core/log/json/detect/state/backup/module libraries, the `apt-base` module, `server`/`desktop`/`hermes` profiles, and `install/bootstrap.sh` + `install/preflight.sh`.
